---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - airport_service.py
  - player_service.py
  - game_logic.py
  - main.py
autonomous: true
requirements:
  - ARCH-04
  - ARCH-02
  - DB-02
must_haves:
  truths:
    - "All six required module files exist at project root"
    - "Every module imports without error (python3 -c 'import airport_service' succeeds)"
    - "No SQL string appears in any of the four stub files"
    - "The import chain is correct: main imports game_logic, game_logic imports services, services import db_manager — no module bypasses the chain"
    - "Each stub has docstrings and correct function signatures matching the Phase 2 contract"
  artifacts:
    - path: "airport_service.py"
      provides: "Stub: get_reachable_airports, get_airport — imports db_manager only"
      exports: ["get_reachable_airports", "get_airport"]
    - path: "player_service.py"
      provides: "Stub: create_player_game, load_player_game — imports db_manager only"
      exports: ["create_player_game", "load_player_game"]
    - path: "game_logic.py"
      provides: "Stub: new_game, load_game — imports airport_service, player_service, config"
      exports: ["new_game", "load_game"]
    - path: "main.py"
      provides: "Stub: entry point — imports game_logic only"
  key_links:
    - from: "airport_service.py"
      to: "db_manager.py"
      via: "import db_manager at top"
      pattern: "^import db_manager"
    - from: "player_service.py"
      to: "db_manager.py"
      via: "import db_manager at top"
      pattern: "^import db_manager"
    - from: "game_logic.py"
      to: "airport_service, player_service, config"
      via: "import at top — NOT importing db_manager directly"
      pattern: "^import airport_service|^import player_service|^import config"
    - from: "main.py"
      to: "game_logic"
      via: "import game_logic — NOT importing db_manager or services directly"
      pattern: "^import game_logic"
---

<objective>
Create the four stub module files that complete the required module structure. These are empty shells with correct imports, function signatures, and docstrings — no game logic. Their purpose is to enforce the layer boundary from day one so Phase 2 never has to retrofit the architecture.

Purpose: ARCH-04 requires all six module files to exist. ARCH-02 requires the import chain to be correct so SQL cannot leak upward. Creating stubs now means the architecture test passes immediately and any Phase 2 violation is caught by the existing tests.
Output: airport_service.py, player_service.py, game_logic.py, main.py — all at project root with correct import chains and function stubs.
</objective>

<execution_context>
@/Users/vlad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vlad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Import chain that MUST be respected — from RESEARCH.md locked decision -->
<!--
main.py
  imports: game_logic ONLY (not db_manager, not airport_service, not player_service)
game_logic.py
  imports: airport_service, player_service, config (NOT db_manager directly)
airport_service.py
  imports: db_manager ONLY (not config directly, not game_logic)
player_service.py
  imports: db_manager ONLY (not config directly, not game_logic)
db_manager.py
  imports: config, mysql.connector (already implemented in plan 02)
config.py
  imports: os, dotenv (already implemented in plan 02)
-->

<!-- Function signatures that Phase 2 will implement (stubs only in Phase 1) -->

airport_service.py:
```python
def get_reachable_airports(current_airport_ident: str, battery_remaining: float) -> list:
    """Return list of airports reachable from current location within battery range.
    Each item is a dict: {ident, name, city, distance_km, battery_cost, speaker_fee, difficulty}
    """
    pass  # Phase 2

def get_airport(ident: str) -> dict | None:
    """Return airport record as dict, or None if not found."""
    pass  # Phase 2
```

player_service.py:
```python
def create_player_game(player_name: str) -> dict:
    """Create a new game for player_name at STARTING_AIRPORT. Returns game dict."""
    pass  # Phase 2

def load_player_game(player_name: str) -> dict | None:
    """Load the most recent game for player_name. Returns game dict or None."""
    pass  # Phase 2
```

game_logic.py:
```python
def new_game(player_name: str) -> dict:
    """Initialize a new game session. Returns initial game state dict."""
    pass  # Phase 2

def load_game(player_name: str) -> dict | None:
    """Load an existing game session. Returns game state dict or None."""
    pass  # Phase 2
```

main.py:
```python
def main():
    """Entry point: display startup menu, route to new_game or load_game."""
    pass  # Phase 4

if __name__ == "__main__":
    main()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create four stub modules with correct import chains</name>
  <files>airport_service.py, player_service.py, game_logic.py, main.py</files>
  <action>
Create all four files at the project root. Follow the import chain exactly — this is ARCH-02 and ARCH-04 compliance from the first commit.

**airport_service.py:**
```python
import db_manager


def get_reachable_airports(current_airport_ident: str, battery_remaining: float) -> list:
    """
    Return a list of airports reachable from current_airport_ident within battery_remaining.

    Each item in the list is a plain dict:
        {
            "ident": str,
            "name": str,
            "city": str,          # municipality column in airport table
            "distance_km": float,
            "battery_cost": float, # distance_km * 0.1
            "speaker_fee": int,
            "difficulty": str,
            "continent": str,
        }

    Uses Haversine formula for distance calculation (implemented in Phase 2).
    Only airports with battery_cost <= battery_remaining are included.
    Implemented in Phase 2.
    """
    pass


def get_airport(ident: str) -> dict | None:
    """
    Return the airport record for a given ident as a plain dict, or None if not found.

    Implemented in Phase 2.
    """
    pass
```

**player_service.py:**
```python
import db_manager


def create_player_game(player_name: str) -> dict:
    """
    Create a new game row for player_name at the starting airport (config.STARTING_AIRPORT).

    Returns the new game record as a plain dict:
        {id, name, money, battery_used, global_awareness, current_airport}

    Delegates to db_manager.create_game(). Implemented in Phase 2.
    """
    pass


def load_player_game(player_name: str) -> dict | None:
    """
    Load the most recent game record for player_name.

    Returns game dict or None if no game found for this name.
    Shows clear error message ("No game found for [name]") when None — handled in game_logic.
    Delegates to db_manager.get_latest_game(). Implemented in Phase 2.
    """
    pass
```

**game_logic.py:**
```python
import airport_service
import player_service
import config


def new_game(player_name: str) -> dict:
    """
    Initialize a new game session for player_name.

    - Creates a game row via player_service.create_player_game()
    - Returns the initial game state dict

    Implemented in Phase 2.
    """
    pass


def load_game(player_name: str) -> dict | None:
    """
    Load an existing game session for player_name.

    - Loads game row via player_service.load_player_game()
    - Returns game state dict, or None if no game found

    Implemented in Phase 2.
    """
    pass
```

**main.py:**
```python
import game_logic


def main():
    """
    Entry point for The Aviator game.

    Displays startup menu:
        1. New game
        2. Continue existing game
        3. Quit

    Routes to game_logic.new_game() or game_logic.load_game() based on selection.
    Input validation and game loop implemented in Phase 4.
    """
    pass


if __name__ == "__main__":
    main()
```

Important: Do NOT add `import db_manager` to game_logic.py or main.py. Do NOT add any SQL strings. The import chain must follow: main → game_logic → services → db_manager → config.
  </action>
  <verify>
    <automated>cd /Users/vlad/dev/the_aviator && python3 -c "
import sys
sys.path.insert(0, '.')

# Test import chain
import airport_service
import player_service
import game_logic
import main

# Verify forbidden imports are absent
import ast, inspect
for mod_name, mod in [('airport_service', airport_service), ('player_service', player_service)]:
    src = inspect.getsource(mod)
    assert 'import game_logic' not in src, f'{mod_name} must not import game_logic'
    assert 'import config' not in src, f'{mod_name} must not import config directly'

game_logic_src = inspect.getsource(game_logic)
assert 'import db_manager' not in game_logic_src, 'game_logic must not import db_manager directly'

main_src = inspect.getsource(main)
assert 'import db_manager' not in main_src, 'main must not import db_manager directly'
assert 'import airport_service' not in main_src, 'main must not import airport_service directly'

print('PASS: All import chain rules satisfied')
" 2>&1</automated>
  </verify>
  <done>
- airport_service.py, player_service.py, game_logic.py, main.py all exist at project root
- Each file imports only its allowed upstream module (per import chain rules)
- No SQL strings in any of the four files
- All four modules import without error
- game_logic.py does NOT import db_manager
- main.py imports ONLY game_logic
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify full architecture test suite passes</name>
  <files>tests/test_architecture.py</files>
  <action>
With all six module files now existing (config.py and db_manager.py from plan 02, four stubs from task 1), run the full architecture test suite. Both tests must pass:

```bash
pytest tests/test_architecture.py -v
```

If test_module_structure fails: a required .py file is missing — check that all six files exist at project root.

If test_sql_only_in_db_manager fails: a SQL keyword leaked into a non-db_manager file — read the violation output and fix.

Also run the security test to confirm the full smoke suite is green:
```bash
pytest tests/test_security.py tests/test_architecture.py -v
```

Both tests must pass with zero failures. These two tests require no DB connection and must always be green from this point forward.

After passing, also verify main.py is runnable as a script (it will do nothing since main() is a stub, but it must not crash):
```bash
python3 main.py
```
Expected: no output, no error (main() is `pass`).
  </action>
  <verify>
    <automated>cd /Users/vlad/dev/the_aviator && python3 -m pytest tests/test_security.py tests/test_architecture.py -v 2>&1</automated>
  </verify>
  <done>
- pytest tests/test_architecture.py -v: 2 tests pass (test_module_structure, test_sql_only_in_db_manager)
- pytest tests/test_security.py -v: 1 test passes (test_no_hardcoded_credentials)
- python3 main.py exits cleanly with no output
- All six module files confirmed present and importable
  </done>
</task>

</tasks>

<verification>
1. `python3 -m pytest tests/test_architecture.py tests/test_security.py -v` — 3 tests pass
2. `python3 -c "import airport_service, player_service, game_logic, main; print('OK')"` — prints OK
3. `python3 main.py` — exits cleanly, no error
4. `grep -rn "SELECT\|INSERT\|UPDATE\|DELETE" airport_service.py player_service.py game_logic.py main.py` — no output
5. `grep -n "import db_manager" game_logic.py main.py` — no output (forbidden imports absent)
</verification>

<success_criteria>
- All six files required by ARCH-04 exist and are importable
- Architecture smoke tests (ARCH-02, ARCH-04) pass without DB access
- Security smoke test (DB-04) passes
- Import chain is correct: main→game_logic→services→db_manager→config
- No SQL in stub files
- Phase 1 is complete: `pytest tests/test_security.py tests/test_architecture.py` is fully green
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
